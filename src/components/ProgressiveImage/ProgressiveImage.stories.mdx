import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { ProgressiveImage } from './ProgressiveImage';

<Meta
  title="Component/ProgressiveImage"
  component={ProgressiveImage}
  parameters={{
    actions: { disabled: true }
  }}
  decorators={[
    (Story) => (
      <div className="flex items-center justify-center">
          <div className="w-full md:w-96 mt-5 mx-6 md:mx-0">
            {Story()}
          </div>
      </div>
    ),
  ]}
  args={{
      src: "https://raw.githubusercontent.com/HybridShivam/Pokemon/master/assets/images/001.png",
      alt: "bulbasaur"
  }}
/>

# SplashScreen

export const Template = (args) => <ProgressiveImage {...args} />;

<Canvas>
  <Story
    name="Default"
  >
    {Template.bind({})}
  </Story>
</Canvas>
